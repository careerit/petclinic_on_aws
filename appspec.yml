version: 0.0
os: linux
files:
  - source: petclinic.war
    destination: /opt/tomcat/webapps/ROOT.war
file_exists_behavior: OVERWRITE
hooks:
  AfterInstall:
    - location: install.sh
      timeout: 300
      runas: root